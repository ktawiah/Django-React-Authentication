import Login from "@/components/layout/login";

const Page = () => {
  return (
    <div className="h-screen flex justify-center items-center">
      <Login />
    </div>
  );
};

export default Page;
